services:
  - type: web
    name: risk-analyzer
    env: python
    rootDir: risk-analyzer
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    plan: free
    envVars:
      - key: PORT
        value: 10000
      - key: PYTHON_VERSION
        value: 3.9.18